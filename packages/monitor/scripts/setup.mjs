// Superseded by the monorepo-wide setup. From the repo root:
//   npm run setup
console.error("This script moved: run `npm run setup` from the stoke repo root (scripts/setup.mjs).");
process.exit(1);
